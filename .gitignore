.DS_Store
build/
__pycache__/
*.pyc
*.swp
*.swo
restart_glidee.sh
Tests
demo
.kiro
demo glide
plan.md
.impeccable
DESIGN.md
PRODUCT.md
NEXT-STEPS.md
glide.zip
glide_minimal.zip
.agents

# Screen recordings and renders are large (the raw capture is ~700MB) and should
# never reach the repo. Patterns rather than names, so a new recording is covered.
*.mp4
*.mov
# Exception: the finished demo is committed so the README can embed it.
!assets/demo.mp4
website
# Local-only tooling: the demo-edit TTS venv alone is ~1GB, and HandOverlay is a
# separate build. Nothing here ships with the app.
tools